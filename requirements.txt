# ── Core (always required) ─────────────────────────────
pydantic>=2.7,<3
pydantic-settings>=2.4
pyyaml>=6.0
httpx>=0.27
tenacity>=9.0
numpy>=1.26
rapidfuzz>=3.10
structlog>=24.4
rich>=13.9
typer>=0.12
python-dotenv>=1.0
beautifulsoup4>=4.12
lxml>=5.3
bibtexparser>=1.4

# ── ML stack (Layer 1 Sentinel + Layer 2 NLI/SRL) ──────
# Install with:  pip install -e ".[ml]"
# torch>=2.2
# transformers>=4.46
# tokenizers>=0.20
# sentence-transformers>=3.0
# safetensors>=0.4
# accelerate>=1.0
# scipy>=1.13

# ── Retrieval extras (Layer 3) ─────────────────────────
# biopython>=1.84   # PubMed E-utilities
# arxiv>=2.1

# ── Document parsing (Layer 3 parsing agent) ───────────
# python-docx>=1.1
# pylatexenc>=2.10

# ── MCP server (Layer 3) ───────────────────────────────
# mcp>=1.0

# ── Dev / Test ─────────────────────────────────────────
pytest>=8.3
pytest-asyncio>=0.24
pytest-cov>=5.0
ruff>=0.7
respx>=0.21
hypothesis>=6.115
